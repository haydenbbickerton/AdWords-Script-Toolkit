/*
	Define our scripts and pull them in.

	The key for each entry needs to be the same as
	the key you used in accounts.config.js.

	Example:
	var scripts = {
		'myScriptName': require('./myScript.js')
	};
*/

var scripts = {};

module.exports = scripts;